<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.victoria.myapplication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ActivityStartScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityOrderingScreen"
            android:label="@string/title_activity_launch_terminal"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

</manifest><!--
 Ich habe Portait orientation festgesetzt, da wir nur diese Orientierung
brauchen und somit uns die Erstellung eines Landscape screens spaaren
-->
